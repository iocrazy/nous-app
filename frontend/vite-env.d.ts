/// <reference types="vite/client" />
// Types `import.meta.env` (VITE_* vars, DEV/PROD/MODE). The project's tsconfig
// `types` array intentionally narrows auto-included @types, so this explicit
// reference is how Vite's client types reach the program.
