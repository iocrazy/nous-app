// App.tsx — Legacy entry point (no longer used)
//
// The application now uses React Router. See:
//   - frontend/router.tsx        — route configuration
//   - frontend/components/AppLayout.tsx — layout (Sidebar + TopBar + Outlet)
//   - frontend/pages/            — page components
//   - frontend/index.tsx          — entry point (RouterProvider)
//
// This file is kept as a stub to avoid breaking any tooling that may
// reference it.  It is safe to delete.

export {};
