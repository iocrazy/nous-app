/**
 * Tag group holding the Transcript / Summary / Analyze system tags. Those AI
 * intents are now explicit request fields, so pickers hide this group instead
 * of offering its tags as ordinary choices.
 */
export const PIPELINE_TAG_GROUP = 'Pipeline';
