# B2 T0 — 数据入口按集过滤（仓库层）报告

## 范围
只改仓库层 `backend/app/repositories/project_stage_nodes_repository.py` + 新增测试。未碰
`advance_service.py`（T1）、前端、migration。

## 改了哪些函数 / 最终签名（供 T1 消费）

### 1. `_list_nodes_in_session`（内部 helper，加可选参数）
```python
async def _list_nodes_in_session(
    self, session, pid: int, episode_id: Optional[int] = None
) -> List[Dict[str, Any]]:
```
`episode_id is not None` 时追加 `WHERE episode_id = :episode_id`。默认 None → 行为不变。

### 2. `list_nodes`（不变）
```python
async def list_nodes(self, project_id: str) -> List[Dict[str, Any]]:
```
签名与行为完全不变（内部以 episode_id=None 调用 helper）。项目全量列表。

### 3. `list_nodes_by_episode`（新增，T1 读取入口）
```python
async def list_nodes_by_episode(
    self, project_id: str, episode_id: str
) -> List[Dict[str, Any]]:
```
- 返回形状：与 `list_nodes` **完全一致** —— `List[_node_row dict]`（含 members / depends_on）。
- 行集限定到该 `episode_id`。`episode_id IS NULL` 的 legacy 项目级节点**被排除**。
- 入参按仓库既有惯例接受 str（内部 `int(str(episode_id))`），与 project_id 一致。
- 选型说明：任务允许「给 list_nodes 加可选参数」或「新方法」二选一。采用**新方法**，
  因为交付要求 T1 消费的正是 `list_nodes_by_episode` 这个名字；同时 `list_nodes` 原样
  保留、零调用方改动。两者共用同一个 `_list_nodes_in_session`，无重复逻辑。

### 4. `get_active_group`（加可选 episode_id，默认行为不变）
```python
async def get_active_group(
    self, project_id: str, episode_id: Optional[str] = None
) -> List[Dict[str, Any]]:
```
- `episode_id=None`（默认）→ 与旧实现逐字节等价（项目级，向后兼容）。
- 给定 episode_id 时，对**两处**查询都加 `episode_id = :eid` 过滤：
  (a) cursor 命中节点的查询（current 节点按集确认）；
  (b) parallel_group 扩散查询（关键：阻止别集同 parallel_group 的模板克隆双胞胎被并入）。
- 全部走 ORM `select()`（本来就不是裸 SQL），**无需 `scoped_sql`**。

## get_active_group 调用方是否动过
**没动。** 唯一调用方 `app/services/workflow/node_mutations.py:85`
（`repo.get_active_group(str(project_id))`）不传 episode_id，走默认分支，行为不变。
新参数是纯 opt-in，避免碰 T1 才该改的调用点。

## 测试（`backend/tests/test_episode_node_filtering.py`，新增）
- 造 **2 集**项目（EP1=8001 / EP2=8002），两集各一节点、`parallel_group` 都是 `"draft"`
  （模拟模板逐字拷贝 → 跨集相同，正是 P0 陷阱的成因）。单集测不出这个 bug。
- FakeSession 沿用 house pattern（test_workflow_deps.py），但**真正对每条查询的
  `_where_criteria` 求值**（支持 `==` / `is_` / `in_` 三个算子），所以「查询忘了 episode 谓词」
  会真的漏出另一集的行 —— 测得到 bug，而非只断言 SQL 文本。
- 4 个用例：
  1. `list_nodes_by_episode(p, EP1)` 只返回 EP1、不含 EP2。
  2. `list_nodes(p)` 仍返回全项目两集（向后兼容）。
  3. `get_active_group(p, episode_id=EP1)` 只含 EP1 节点、排除 EP2 同 parallel_group 双胞胎。
  4. 反向对照：`get_active_group(p)`（不传 episode）在同一 fixture 下把两集双胞胎并进同一
     active group —— 证明 fixture 确实在做过滤、且这正是 T0 修的 P0 bug。

### TDD 证据
临时把两处 episode 过滤条件短路（`if False:`）后跑：用例 1、3 **FAILED**，用例 2、4 PASSED
（证明测试真的咬住了过滤逻辑、且 fixture 有效）。恢复后 4/4 PASSED。

### pytest 结果
- `tests/test_episode_node_filtering.py`: **4 passed**。
- 相关回归（test_workflow_deps / test_workflow_episode_scoping / test_advance_predicate /
  test_episode_node_filtering / api/test_project_workflow_attach）: **80 passed**，无回归。
- `black --check` + `isort --check-only` 两文件均 clean。

## 顾虑 / 交接给 T1 的点
- `get_active_group` 给 episode_id 时，cursor 命中查询也按集过滤：若 cursor 指向的节点不属于
  该集，返回 `[]`（该集此刻无 active group）。这是按集读的正确语义，但 T1 下沉 cursor 到
  episode 级（per-episode current_node_id）后，要确认传进来的 episode_id 与 cursor 语义一致。
- 目前 `projects.current_node_id` 仍是项目级单一 cursor；T1/后续 task 若引入
  `episodes.current_node_id`，`get_active_group` 的 cursor 来源需相应调整（本 task 不涉及）。
- `list_nodes_by_episode` 排除 `episode_id IS NULL` legacy 节点。若某项目是 B3 之前建的、
  节点都是 NULL episode，按集查会返回空 —— 这是预期（legacy 项目走 `list_nodes` 老路径）。
