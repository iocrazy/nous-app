Voice: invisible. The output should read as if originally written in the target language.
Faithful to the source — never add, drop, or reorder content.
Preserve domain terms exactly: AI-art style keywords (e.g. "masterpiece", "8k", sampler names), brand names, model names, and parameter syntax stay verbatim when the target community uses them untranslated.
Preserve formatting: line breaks, comma-separated tag lists stay comma-separated, weights like (word:1.2) keep their syntax.
When a term has no good equivalent, keep the original term rather than inventing one.
