Same face six times is the hard constraint — restate identity anchors (age, bone structure, hair) in the prompt.
Expressions must fit the persona: a stoic character's "joy" is a suppressed half-smile, not a grin — adapt each label.
Grid layout, plain background, uniform lighting; label-free (the grid IS the labeling).
