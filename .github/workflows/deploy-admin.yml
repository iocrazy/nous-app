# Admin Panel Deploy to NAS (local build)
# Triggers:
#   - Push to master with admin/ changes
#   - Manual trigger

name: Deploy Admin Panel to NAS

on:
  push:
    branches: [master]
    paths:
      - 'admin/**'
  workflow_dispatch:

concurrency:
  group: deploy-admin
  cancel-in-progress: true

jobs:
  deploy:
    name: Build & Deploy on NAS
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to NAS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.NAS_HOST }}
          port: ${{ secrets.NAS_PORT }}
          username: ${{ secrets.NAS_USER }}
          key: ${{ secrets.NAS_SSH_KEY }}
          script: |
            export PATH="/usr/local/bin:/usr/bin:/bin:$PATH"
            cd ${{ secrets.NAS_PROJECT_PATH }}

            echo "📥 Pulling latest code..."
            git pull origin master

            echo "🔨 Building Admin Panel..."
            cd docker
            sudo docker compose build --no-cache mediahub-admin

            echo "🚀 Restarting Admin Panel..."
            sudo docker compose up -d --force-recreate mediahub-admin

            echo "🧹 Cleaning up..."
            sudo docker image prune -f

            echo "✅ Admin Panel deployed on port 3097"
