# Schema-Drift Gate — build the schema in an ephemeral Postgres, then diff it
# against the SQLAlchemy ORM models.
#
# ─────────────────────────────────────────────────────────────────────────
# WHAT THIS DOES
# ─────────────────────────────────────────────────────────────────────────
#   1. Boot a throwaway Postgres 17 (matches prod's 17.6).
#   2. supabase/ci_bootstrap.sql    — stub the objects Supabase/DBOS create at
#                                     runtime (auth schema, roles, extensions…).
#   3. supabase/schema_baseline.sql — prod's real applied public schema, as of
#                                     the watermark recorded in its header.
#   4. Apply ONLY migrations numbered ABOVE that watermark (*_rollback.sql
#      excluded — those only ever run manually).
#   5. Run backend/tests/db/test_schema_drift.py against the result.
#
# So a migration that changes the schema without the models being regenerated
# fails HERE — on the PR, before it ever reaches prod.
#
# ─────────────────────────────────────────────────────────────────────────
# WHY BASELINE + INCREMENT (AND NOT "REPLAY ALL MIGRATIONS")
# ─────────────────────────────────────────────────────────────────────────
# Because replaying them does not rebuild prod, and cannot: the file numbering
# is NOT the order in which these migrations reached prod, and several are
# invalid as committed. Measured 2026-07-15 — a full in-order replay lands at
# ~346/371 applied and produces a database with `unified_tasks` and NO
# `task_tracking`, i.e. the opposite of prod on the single most important table.
# Diffing models against that would be confidently wrong. The long-form
# evidence is at the top of supabase/schema_baseline.sql.
#
# ─────────────────────────────────────────────────────────────────────────
# ZERO SECRETS — AND NO SILENT SKIPS
# ─────────────────────────────────────────────────────────────────────────
# Everything this job needs is committed to the repo, so it runs identically on
# forks. It touches NO production system and needs NO secret.
#
# This replaced a gate that reported green for months without checking anything:
# its final step read `if [ -z "$PROD_DB_PASSWORD" ]; then ... exit 0; fi`, and
# that secret was never configured. A guard that silently no-ops is worse than
# no guard — it manufactures the impression of coverage.
#
# THEREFORE, THE RULE FOR THIS FILE: there is no "can't run → pass" path.
# Every failure mode (Postgres won't boot, bootstrap fails, the baseline won't
# apply, a migration errors, the watermark can't be parsed) is RED. Do not add
# `continue-on-error`, `|| true`, or a conditional `exit 0` to any step here.
#
# THAT RULE APPLIES TO THE PYTEST STEPS TOO — and for a long time it did not.
# `pytest` exits 0 when every test in a file skips, and every integration file
# below skips itself (module-level or per-case) when INTEGRATION_DATABASE_URL
# is unset. A bare `uv run pytest <file>` step therefore went GREEN while
# asserting nothing: the same "manufactured impression of coverage" this
# workflow replaced, one layer down. So EVERY integration step below runs
# through .github/scripts/pytest-no-full-skip.sh, which fails the step when the
# summary reports zero passing tests. Adding a new integration step here means
# calling that script — never a bare `uv run pytest`.

name: Schema Drift (models vs baseline + migrations)

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'backend/app/models/**'
      - 'supabase/migrations/**'
      - 'supabase/schema_baseline.sql'
      - 'supabase/ci_bootstrap.sql'
      - 'backend/tests/db/test_schema_drift.py'
      - 'backend/tests/db/test_assets_repository_integration.py'
      - 'backend/tests/db/test_canvas_asset_refs_integration.py'
      - 'backend/tests/db/test_conversations_store_question_integration.py'
      - 'backend/tests/db/test_inbox_expire_skips_paused_integration.py'
      - 'backend/tests/db/test_run_deliverables_repository_integration.py'
      - 'backend/tests/db/test_generated_media_promoted_lookup_integration.py'
      - 'backend/tests/db/test_migration_466_revert_cost_integration.py'
      - 'backend/tests/db/test_revert_output_integration.py'
      - 'backend/tests/db/test_step_costs_and_media_price_integration.py'
      # The two modules that file executes (3b Task 4). Both are single ORM
      # statements whose unit tests hand a STUBBED session a hand-built list of
      # dicts — so the step below is the only place Postgres ever sees them,
      # and the only place that can answer whether a JSONB payload's
      # cost_cents survives the driver, whether NUMERIC per_call_cents arrives
      # as a Decimal the read side must float(), or which row
      # `ORDER BY effective_at DESC` + `per_call_cents IS NOT NULL` picks.
      - 'backend/app/services/deliverables/step_costs.py'
      - 'backend/app/services/deliverables/media_price.py'
      - 'backend/tests/db/test_run_seq_and_output_keys_integration.py'
      # 3b Task 4b 的两个聚合读。两边的单测都把这两个方法整个打桩，所以
      # 除了下面那一步，没有任何地方让 Postgres 见过它们。
      - 'backend/app/repositories/agent_runs_repository.py'
      - 'backend/tests/db/test_transcript_seq_conflict_integration.py'
      # The writer whose seq-conflict retry that file is the only real-driver
      # proof of: a stub can raise IntegrityError on command, but only Postgres
      # + asyncpg answer whether a UNIQUE (run_id, seq) violation actually
      # ARRIVES as one at the except clause.
      - 'backend/app/services/ai/runner/run_recorder.py'
      # Lives under tests/integration/, not tests/db/ — the step below names it
      # explicitly, so this entry is what makes a change to it trigger a run.
      - 'backend/tests/integration/test_agent_workforce_repository_orm.py'
      # The repository whose statements that file executes. Its unit tests stub
      # the session, so a change here reaches Postgres nowhere else.
      - 'backend/app/repositories/agent_workforce_repository.py'
      # The statements that integration file executes (phase 2a question stamp).
      - 'backend/app/services/ai/chat/conversations_ai_store.py'
      - 'backend/app/repositories/agent_run_inbox_repository.py'
      # The deliverables repository + the registry's retry loop. Both are
      # pure ORM/behaviour whose unit tests stub the session, so the step
      # below is the only place Postgres ever sees them (3a Task 2).
      - 'backend/app/repositories/run_deliverables_repository.py'
      - 'backend/app/services/deliverables/registry.py'
      # 同上一条：promoted-resource 反查的 ORM 语句只在下面那一步真正执行，
      # 它的单测 session 是桩的。只改这个模块的 PR 如果不触发本 job，那条
      # 真 PG 用例就跑不到 —— 等于悄悄失去它唯一的门禁。
      - 'backend/app/repositories/generated_media_repository.py'
      # 3b Task 3 的回退服务：本仓第一个会改**用户内容**的产出写入方。它的单测把
      # 六个外部面全桩掉了，所以在下面那一步之前，它的 `SELECT … FOR UPDATE`、
      # 人手账本行（`run_id NULL` + `actor`，靠 mig 466 的
      # `script_shot_ops_run_or_actor` 放行）、以及「登记失败要把内容一起回滚」
      # 这条事务承诺，Postgres 一次都没看见过。`diff.py` 同批：``rebuild_content``
      # 的返回值会被回退**直接 UPDATE 进 script_shots**，形状错了没有单测会红。
      - 'backend/app/services/deliverables/revert.py'
      - 'backend/app/services/deliverables/diff.py'
      # ⚠️ COVERAGE GAP, STATED PLAINLY. This entry was added because the
      # project-entity migration workflow imported `_NOT_MARKABLE` and
      # `_resources_with_asset_files_stmt` from this module, and the step that
      # ran that workflow (`_run_extra_steps` -> `_map_generated_media`) was
      # the ONE place `_resources_with_asset_files_stmt()` actually executed
      # against a real Postgres. Mig 451 deleted that workflow and its step, so
      # NO step in this job executes those definitions any more — the surviving
      # test only COMPILES the statement against a stubbed session. The entry
      # stays so a change here still triggers the schema build below rather
      # than silently losing its only gate, but do not read a green run as
      # "the server accepted this statement". The mig-450 step added by #2108
      # does NOT close this: it exercises chat_upload.py, not this module
      # (checked — no step file imports `backfill_generated_inbox`). Closing
      # the gap means giving it its own live integration case here.
      - 'backend/app/workflows/backfill_generated_inbox.py'
      # The chat-uploads folder is identified by `system_key`, and the
      # criterion the backfill joins on lives in chat_upload.py. Migration 450
      # backfills that identity, and its candidate predicate must stay
      # identical to the one in chat_upload.py — so a change to either has to
      # reach this chain. ⚠️ Unlike the entry ABOVE, this one IS executed live:
      # the mig-450 step runs `_ensure_chat_uploads_folder` against the built
      # schema. Do not read the two comments as making the same promise.
      - 'backend/app/services/library/chat_upload.py'
      - 'backend/tests/db/test_chat_uploads_folder_migration.py'
      - 'backend/app/repositories/assets_repository.py'
      - 'backend/app/repositories/asset_relations_repository.py'
      - 'backend/app/repositories/canvas_asset_refs_repository.py'
      # The prompt catalog's OTHER half (templates come from
      # assets_repository above). Its membership rule IS has_prompt_expr,
      # which lives in media_repository — a change to either can only be
      # settled by running it, so both entries point at the same step.
      - 'backend/app/repositories/prompt_catalog_repository.py'
      - 'backend/app/repositories/media_repository.py'
      - 'backend/tests/db/test_prompt_catalog_repository_integration.py'
      # 初始化标签每人一份（mig 468）。改这个测试或 tags 模型都要重跑：
      # 每人一份靠的是 (user_id, slug) 这条 partial unique，只有真库能回答。
      - 'backend/tests/db/test_migration_468_tag_presets_integration.py'
      # 注册用户名推导（mig 469）。唯一约束 + NOT NULL + 触发器失败连带回滚
      # auth.users 行 —— 三件都只有真库能回答。
      - 'backend/tests/db/test_migration_469_signup_username_integration.py'
      # 主账号名唯一性（mig 470）。函数唯一索引 + NOT NULL + 两条注册路径
      # 共用的 PL/pgSQL —— 只有真库能回答。
      - 'backend/tests/db/test_migration_470_username_integration.py'
      # 3c mig 472：两张新表 + 两组计数列。trgm opclass 在不在、UNIQUE 拦不拦得住
      # 第二行、五个累加器是不是 NOT NULL DEFAULT 0 —— 只有真库能答。
      - 'backend/tests/db/test_mig_472_integration.py'
      # mig 475：关键词搜索必须应用筛选 chip。缺陷与修复都在 SQL 函数里——桩 session
      # 能证明后端传了 p_ai_transcribed，只有 Postgres 能证明没转录的行真的不回来。
      # service 的 SQL 串（命名参数 + CAST）也只在这里第一次见到真库，故 service
      # 改动同样要触发。
      - 'backend/tests/db/test_migration_475_search_chip_filters_integration.py'
      - 'backend/app/services/library/search_service.py'
      # mig 478：笔记归档。「移出视线」是四个查询的共同主张，其中两个是 SQL
      # 函数（inspiration_tag_counts / inspiration_activity）——桩能证明后端调了
      # 它们，只有 Postgres 能说出归档笔记是不是还被数在里面。
      - 'backend/tests/db/test_migration_478_inspiration_archive_integration.py'
      - 'backend/tests/db/test_search_docs_repository_integration.py'
      # 上面那个文件读迁移建出来的形状；这个读 SearchDocsRepository 的语句在
      # 服务器上跑得通不通（similarity / ON CONFLICT / Uuid 双绑定 / ILIKE
      # ESCAPE）。所以 repository 与投影写方改动也要能触发本 job。
      - 'backend/app/repositories/search_docs_repository.py'
      - 'backend/app/services/search/**'
      # 存量正文回填（3c 补充票）。它重建正文走的是 deliverables/diff.py 的账本
      # 折叠 + 两个渲染器（都已在上面列着），写回去走 fill_empty_body —— 而
      # 「只补空的、不动 updated_at」这两条承诺只有真库能证明。
      - 'backend/tests/db/test_search_docs_body_backfill_integration.py'
      - 'backend/scripts/backfill_search_docs_body.py'
      # 上面两个文件各自证明一层；这个证明**两层合起来拦得住人**：
      # /api/v1/search 是跨团队端点，它唯一的安全断言（别 team 的人搜同一个词
      # 拿三组全空）在单测里是桩的。两道门各有一条专属用例，实测各自删掉会让
      # 不同的那一条转红。issue 可见性的实现也在它的射程内。
      - 'backend/tests/db/test_unified_search_visibility_integration.py'
      - 'backend/app/services/issues/issue_visibility.py'
      - 'backend/app/repositories/issue_repository.py'
      # 同样的道理，引用反查那一侧（3c Task 16）：ON CONFLICT DO NOTHING 要
      # output_citations_message_ref_key 真存在，ORDER BY 的决胜键要真的定住同一
      # 事务里的两条，GROUP BY 要真按版本分组。
      - 'backend/tests/db/test_output_citations_repository_integration.py'
      - 'backend/app/repositories/output_citations_repository.py'
      - 'backend/app/services/deliverables/citations.py'
      # 积分收口的 CAS（积分 Task 3）。幂等全挂在 root 行那一个
      # metadata_json.cost.charged_at 上：where 要同时匹配「cost 层在」与「cost
      # 层不在」两种形状，values 的两层 jsonb || 写错不会报错、只会把 cost 视图
      # 冲掉，而钱已经扣了。三件事都只有真服务器判得了。
      - 'backend/tests/db/test_tree_charge_cas_integration.py'
      - 'backend/app/services/ai/billing/tree_charge.py'
      - 'backend/app/services/ai/billing/pre_cutover_refund.py'
      # 退款抵扣读方（终审 I6）。两条腿的谓词、``consume`` 负 / ``refund`` 正这
      # 两个符号（mig 123 的 RPC 定的，不是代码里的约定）、以及 ``amount`` 是
      # INTEGER —— 桩 session 对这三件事一无所知：它按调用顺序发牌，根本不看
      # WHERE，所以把 refund 那条腿的 reference_type 写错单测照样绿而真库交出
      # 零行（退款不抵扣，回到缺陷本身）。
      - 'backend/tests/db/test_points_refund_offset_integration.py'
      - 'backend/app/repositories/points_repository.py'
      - 'backend/app/services/billing/run_tree_points.py'
      # own_cost_cents 的三档回填与子 run 的 issue_id 补齐（3d 第 0 票）。整条
      # 迁移就是四条 UPDATE 的谓词，而谓词正是桩 session 看不见的东西：jsonb 的
      # ``?`` 与 ``->>`` 取值、把有后代的行挡在 tier 2 之外的 NOT EXISTS、以及
      # 「已经有值就不许再写」这三件，全在服务器那边。
      - 'backend/tests/db/test_migration_479_own_cost_backfill.py'
      # mig 488 的数据改名（mediahub → nous）：jsonb_set 改到哪条路径、孪生行守卫、
      # text[] 重建保序、SET LOCAL replica 只压住那一条 UPDATE —— 全是服务器说了算。
      - 'backend/tests/db/test_migration_488_rename_data_integration.py'
      # mig 491：后端专用 SECURITY DEFINER 函数对浏览器角色的执行权 —— 权限只有
      # Postgres 自己答得出，单测走后端会话，永远看不见 PostgREST 那条路。
      - 'backend/tests/db/test_migration_491_revoke_definer_rpcs_integration.py'
      # mig 496：被 RLS 策略引用的 4 个 definer 身份助手只替调用者自己回答 —— 守卫读
      # role GUC 与 JWT claims，只有真库在 anon / authenticated 角色下才测得出来。
      - 'backend/tests/db/test_migration_496_rls_helper_identity_guard_integration.py'
      # mig 497：resource_embeddings（halfvec 2048 + HNSW）与两个 repository。
      # halfvec 的 bind、ON CONFLICT 的目标、「同一空间才比」的 RPC、浏览器角色没有
      # EXECUTE —— 单测的 session 是桩的，这些只有 Postgres 答得出。
      - 'backend/tests/db/test_migration_497_resource_embeddings_integration.py'
      - 'backend/app/repositories/resource_embeddings_repository.py'
      - 'backend/app/repositories/embedding_space_repository.py'
      # own_cost_cents 的四条不变量（3d 第 0 票 PR-B）。六个读面换了列之后，「按树
      # / 按议题 / 按 agent 求和给同一个数」只有在一棵真树上才成立得了：三条语句的
      # WHERE 与 GROUP BY 毫无共同之处，而桩 session 按调用顺序发牌，三条都写错成
      # 同一个错数照样绿。所以被测的四个读方也要挂在这条链上。
      # 被测的四个读方里三个在 agent_runs_repository.py —— 它已经在上面（3b Task
      # 4b 那组）列过了，paths 是「任一命中即触发」，重列一遍只会让下次有人删上面
      # 那行时以为还有人兜着。第四个（issue_totals）在 usage_repository.py。
      - 'backend/tests/db/test_own_cost_tree_invariants_integration.py'
      - 'backend/app/repositories/usage_repository.py'
      # ORM 索引门禁（C1）。这个文件自己就是门禁的全部实现 —— 它的豁免名单
      # 一放宽，整条索引对账就随之失效，所以改它必须重跑它。
      - 'backend/tests/db/test_orm_indexes_integration.py'
      - 'backend/app/services/assets/**'
      # The canvas→asset mirror is written by CanvasService on save and rebuilt
      # by the backfill; both are exercised by the E2 case below, so a change to
      # either has to reach this chain — the ON CONFLICT arm and the
      # array_agg(DISTINCT) can only be settled by a real Postgres.
      - 'backend/app/services/canvas/asset_node_refs.py'
      - 'backend/app/services/canvas/canvas_service.py'
      - 'backend/scripts/backfill_canvas_asset_refs.py'
      - '.github/workflows/schema-drift.yml'
      # The full-skip guard every integration step below runs through. A change
      # to it changes what "green" means for all forty, so it has to
      # re-run them — otherwise the guard could be weakened by a PR this job
      # ignores.
      # ⚠️ That count has been wrong four times (it said "eight" when there
      # were seven, "nine" when there were eight, "eleven" when there were
      # thirteen, then "thirteen" when there were fifteen — each edit trusted
      # the previous number instead of counting). Recount, do not increment
      # (the thirty-nine below was counted with this command, not derived
      # from the thirty-eight it replaced):
      #   grep -cE '^\s*bash .*pytest-no-full-skip\.sh" tests/' .github/workflows/schema-drift.yml
      - '.github/scripts/pytest-no-full-skip.sh'
  push:
    branches: [master]
    paths:
      - 'backend/app/models/**'
      - 'supabase/migrations/**'
      - 'supabase/schema_baseline.sql'
      - 'supabase/ci_bootstrap.sql'

permissions:
  contents: read

concurrency:
  group: schema-drift-${{ github.ref }}
  cancel-in-progress: true

jobs:
  drift:
    name: Build ephemeral schema & diff ORM models
    runs-on: ubuntu-latest

    services:
      postgres:
        # pgvector/pgvector rather than supabase/postgres: the supabase image's
        # entrypoint expects the full platform environment and is not built to
        # run as a bare service container. Its advertised advantage (bundled
        # roles/extensions) does not apply either — auth.users is created by
        # gotrue at RUNTIME, not by the image, and the baseline has 70 FKs to
        # it, so ci_bootstrap.sql must provide it regardless. Given that, the
        # smaller image that pins prod's major version is the better trade.
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: drift
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      PGHOST: 127.0.0.1
      PGPORT: '5432'
      PGUSER: postgres
      PGPASSWORD: postgres
      PGDATABASE: drift

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 上面 paths 那条注释里的数词已经错过四次，每次靠的都是「请重数」这句话。
      # 三行把它变成门禁：grep 数实际有多少个真库 step，从注释里抽那个英文数词，
      # 不等就红。放在最前面 —— 它不需要数据库，一秒钟给答案。
      - name: The step count in the comment must match the steps that exist
        run: |
          have=$(grep -cE '^\s*bash .*pytest-no-full-skip\.sh" tests/' .github/workflows/schema-drift.yml)
          said=$(grep -oE 'what "green" means for all [a-z-]+' .github/workflows/schema-drift.yml | head -1 | awk '{print $NF}')
          python3 -c 'import sys; o="zero one two three four five six seven eight nine ten eleven twelve thirteen fourteen fifteen sixteen seventeen eighteen nineteen".split(); t={"twenty":20,"thirty":30,"forty":40,"fifty":50,"sixty":60,"seventy":70,"eighty":80,"ninety":90}; v=lambda w:sum(o.index(p) if p in o else t.get(p,-10**6) for p in w.split("-")); s,h=sys.argv[1],int(sys.argv[2]); sys.exit(0 if v(s)==h else "::error::schema-drift.yml says \"{}\" ({}) real-database steps but there are {}. Recount, do not increment.".format(s,v(s),h))' "$said" "$have"

      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - uses: actions/setup-python@v5
        with:
          # 同 ci.yml：版本唯一来源是 .python-version（3.13），不在此重抄一份
          python-version-file: .python-version

      - name: Install backend dependencies
        working-directory: backend
        run: uv sync --frozen

      - name: Install psql client
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq postgresql-client

      - name: Wait for Postgres
        run: |
          set -euo pipefail
          for i in $(seq 1 30); do
            if pg_isready -q; then
              echo "Postgres ready after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "::error::Postgres service did not become ready within 30s"
          exit 1

      - name: Apply ci_bootstrap.sql (runtime-owned stubs)
        run: |
          set -euo pipefail
          psql -v ON_ERROR_STOP=1 -q -f supabase/ci_bootstrap.sql
          echo "Bootstrap applied."

      - name: Apply schema_baseline.sql (prod's applied schema)
        run: |
          set -euo pipefail
          psql -v ON_ERROR_STOP=1 -q -f supabase/schema_baseline.sql
          count=$(psql -tAc "SELECT count(*) FROM information_schema.tables
                             WHERE table_schema='public' AND table_type='BASE TABLE';")
          echo "Baseline applied — ${count} public BASE TABLEs."
          # A baseline that applies but yields almost nothing means the file is
          # truncated or corrupt. Fail loudly rather than diff against a stub.
          if [ "$count" -lt 100 ]; then
            echo "::error::Baseline produced only ${count} tables — expected >=100. Corrupt or truncated?"
            exit 1
          fi

      - name: Apply migrations newer than the baseline watermark
        run: |
          set -euo pipefail

          # The watermark lives in the baseline header:
          #   --   BASELINE WATERMARK: 364
          watermark=$(grep -m1 -oE '^--[[:space:]]+BASELINE WATERMARK:[[:space:]]*[0-9]+' \
                        supabase/schema_baseline.sql | grep -oE '[0-9]+$' || true)
          if [ -z "$watermark" ]; then
            echo "::error::Could not parse 'BASELINE WATERMARK: <n>' from supabase/schema_baseline.sql."
            echo "The gate cannot know which migrations to apply. Fix the header."
            exit 1
          fi
          echo "Baseline watermark: ${watermark}"

          # Forward migrations numbered strictly above the watermark, in numeric
          # order. *_rollback.sql are reverse migrations — manual-only, never
          # auto-applied (same rule as run-migration.yml).
          files=$(
            ls supabase/migrations \
              | grep -E '^[0-9]{3}_.*\.sql$' \
              | grep -Ev '_rollback\.sql$' \
              | awk -F_ -v w="$watermark" '$1 + 0 > w' \
              | sort
          )

          if [ -z "$files" ]; then
            # Not a skip: the baseline IS the schema under test, and the drift
            # test still runs against it in the next step.
            echo "No migrations newer than watermark ${watermark}; testing the baseline as-is."
            exit 0
          fi

          echo "Applying $(echo "$files" | wc -l) migration(s) above the watermark:"
          for f in $files; do echo "  - $f"; done

          for f in $files; do
            echo ">> $f"
            psql -v ON_ERROR_STOP=1 -q -f "supabase/migrations/$f"
          done
          echo "All incremental migrations applied."

      - name: Diff ORM models against the built schema
        working-directory: backend
        env:
          # asyncpg.connect() wants a plain libpq DSN (the test uses asyncpg
          # directly, not SQLAlchemy) — no '+asyncpg' driver suffix here.
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # Drift → the test fails → this step fails → the job is RED. That is
          # the entire point of the gate. Known, registered drift is handled by
          # the ratchet allowlists inside the test itself.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_schema_drift.py -v

      - name: Diff ORM-declared indexes against the built schema (C1)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 上一步比表 / 列 / 可空 / 类型 / 外键 —— 唯独不比索引。于是
          # `__table_args__` 里的 `Index(...)` 是这套模型里唯一没有任何东西
          # 对账的声明：指着一个从来没建过的索引，两边都不会有人说话。
          # 方向是单向的（ORM→库零容忍；库→ORM 只计数打印），理由与已知
          # 漂移的逐条豁免都写在那个文件顶部。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_orm_indexes_integration.py -v

      - name: Assert social_accounts' Realtime/RLS + credential grants (mig 417)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # Runs here rather than in ci.yml because these assertions need a real
          # Postgres with the migrations applied — exactly what this job already
          # built. Named explicitly (not a directory glob) for the same reason
          # the step above is: a test file that silently stops being collected
          # is the failure mode this whole workflow exists to prevent.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_social_accounts_realtime_rls.py -v

      - name: Assert the publish read-back schema + sweep SQL (mig 418)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # P1-3's SQL is the part unit tests cannot vouch for: a correlated
          # array_agg subquery and a NULLS FIRST ordering both COMPILE happily
          # under SQLAlchemy and can still be rejected by the server. These
          # execute them for real against the schema this job just built.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_publish_readback_db.py -v

      - name: Assert the asset-library schema's RLS, partial indexes & CHECKs (mig 445/446)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # Complements (does not duplicate) the drift test above: RLS being on
          # with a service_role-only policy, the WHERE predicates that make
          # uq_assets_scope_type_name / uq_loadout_default partial, and the
          # CHECK bodies are all invisible to a model-vs-schema diff — drop the
          # predicate and the drift test still passes while a soft-deleted row
          # squats its name forever.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/migrations/test_445_asset_library_core.py -v

      - name: Exercise the asset-library repositories against the built schema (mig 445/446)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place the two asset repositories' ORM statements actually
          # execute. Their unit tests stub the session, so nothing else proves
          # that asyncpg's IntegrityError names uq_assets_scope_type_name, that
          # UPDATE...RETURNING(Assets) yields an entity, that the partial unique
          # indexes free/hold names, that ON CONFLICT finds the real index, or
          # that a raise inside unit_of_work() rolls the whole batch back.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_assets_repository_integration.py -v

      - name: Exercise the deliverables repository against the built schema (mig 453/462)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place RunDeliverablesRepository's ORM statements execute.
          # Its unit tests stub the session, so nothing else proves that
          # INSERT...RETURNING(RunDeliverables) yields an entity, that NUMERIC
          # cost_cents leaves as a JSON-safe float while BIGINT ids leave as
          # strings, that the 462 unique index really rejects a duplicate
          # (kind, ref_id, version) — the registry's whole race story — or that
          # the two agent_runs JOINs produce the order Task 3 paginates on.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_run_deliverables_repository_integration.py -v

      - name: Exercise the playback-position repository against the built schema (mig 473)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place PlaybackPositionsRepository's ORM statements execute.
          # Its unit tests stub the session, so nothing else proves that
          # ON CONFLICT finds the (user_id, media_key) unique index rather than
          # appending a row per heartbeat, that updated_at actually MOVES on the
          # UPDATE arm (that timestamp is how the client tells its own write
          # from another device's — a frozen one breaks sync silently), or that
          # the UserScoped choke point really filters by user_id. That last one
          # is viewing history: a missing filter is the whole risk.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_playback_positions_integration.py -v

      - name: Exercise the account name — unique, editable (mig 470)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 主账号名：系统发一个、用户能改、全局唯一。三件里有两件是数据库对象，
          # 桩 session 回答不了：「唯一且大小写不敏感」是一条 lower(username) 的
          # 函数唯一索引（既有的 user_profiles_username_key 是大小写敏感的，
          # iocrazy 与 IOCrazy 能并存 —— 对一个用来认人的名字那是可冒充的）；
          # 「每个账号都有名字」是 NOT NULL。
          #
          # 还有一条只有真跑触发器才看得见：注册的两条路径（handle_new_user 与
          # 后端的 _bootstrap_user）现在共用 public.unique_username()，而它是
          # PL/pgSQL。以前两边各写一份，Python 那份是 `... or "user"`，第二个没
          # 邮箱的人就撞唯一约束、异常被吞、profile 悄悄没建成。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_migration_470_username_integration.py -v

      - name: Exercise the signup username derivation (mig 469)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # handle_new_user 是 auth.users 的 AFTER INSERT 触发器，它抛什么都会把
          # 用户行一起回滚 —— 失败的不是「资料没建好」，是「注册不了」。两种形状
          # 会让它抛：没有 email（团队名算出 NULL；生产的手机号与匿名注册都开着）、
          # 以及 email 本地部分撞车（user_profiles.username 唯一，而那句
          # ON CONFLICT (id) 盖不住它）。
          #
          # 每条都先用**迁移前的函数**跑一遍并断言它失败，所以这些是回归钉，
          # 不是对当前行为的描述。只有真库能回答：NOT NULL、UNIQUE、以及触发器
          # 失败会不会连带回滚 auth.users 行。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_migration_469_signup_username_integration.py -v

      - name: Exercise the per-user initial tags (mig 468)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 每一条断言问的都是 Postgres 对象，桩 session 回答不了：两个用户能不能
          # 同时持有一个 slug 取决于唯一索引（467 的全局索引下第二个用户直接被拒）、
          # 「没有系统标签了」是 CHECK、写策略掉没掉 type 是 pg_policy、merge_tags
          # 还能不能被浏览器调到是 ACL，而 seed_initial_tags 本身是 PL/pgSQL。
          #
          # 还有一条只有这里能看见：on_auth_user_created 挂在 auth.users 上，
          # baseline 只 dump public、CI 靠 ci_bootstrap 造这张表，所以这个触发器
          # 从来没进过门禁数据库 —— 「新用户注册就有标签」这句话此前无人可证。
          # mig 468 显式建它，这一步才断言得了。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_migration_468_tag_presets_integration.py -v

      - name: Exercise the promoted-resource reverse lookup (mig 307/456)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 资源信息面板「谁做的我」唯一的解析口。单测的 session 是桩的，
          # 所以 NULL 指针不匹配、BIGINT id 出闸变 string、以及 456 的
          # partial unique 还在，只有这里会被 Postgres 真正回答。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_generated_media_promoted_lookup_integration.py -v

      - name: Exercise the read-time step-cost fold & per-call price lookup (3b Task 4)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place step_costs.load_step_shares and
          # media_price.media_price_cents execute. Their unit tests stub the
          # session, so nothing else proves that a JSONB payload's cost_cents
          # comes back as a usable number, that real NULL turn/step rows
          # (pre-mig-453) are skipped rather than crashing the fold, that
          # `run_id IN (...)` really partitions two runs sharing one (turn,
          # step), that NUMERIC(12,4) per_call_cents arrives as a Decimal the
          # read side has to float(), or that a newer token-only price row
          # does not hide the per-call price the media lane depends on.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_step_costs_and_media_price_integration.py -v

      - name: Exercise the transcript seq-conflict retry against the built schema (mig 397)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place RunEventWriter's seq-conflict retry meets a real
          # unique index. Its unit test builds one in a stub and raises
          # IntegrityError by hand, which pins the retry LOGIC but says nothing
          # about the question that matters: does a UNIQUE (run_id, seq)
          # violation actually reach `except IntegrityError` through asyncpg +
          # SQLAlchemy, carrying sqlstate 23505? If it arrived as anything
          # else the retry would never fire, the unit tests would stay green,
          # and production would keep swallowing one transcript event per
          # deliverable registered mid-turn (真栈 run 348401200407189).
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_transcript_seq_conflict_integration.py -v

      - name: Exercise the canvas→asset ref mirror against the built schema (mig 445)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place CanvasAssetRefsRepository's ORM statements execute.
          # Three properties are invisible to a stubbed session: that
          # ON CONFLICT (canvas_id, asset_id, node_id) DO UPDATE SET loadout_id
          # finds the real PK and actually refreshes a re-saved node's loadout
          # (DO NOTHING would keep the stale one and still report success),
          # that array_agg(DISTINCT ...) really dedupes (two nodes sharing one
          # loadout must contribute that id once), and that the scope filter's
          # COALESCE resolves a personal project through a real LEFT JOIN on
          # teams — a canvas invisible to its own owner would read as "this
          # asset is used nowhere".
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_canvas_asset_refs_integration.py -v

      - name: Exercise the canvas refs archived-output scope filter (mig 445)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place promoted_resource_ids' scope join executes. Its unit
          # tests compile the SQL and feed hand-made row tuples, which cannot
          # settle: that resource_items really carries a resource's scope under
          # those column names (a wrong one compiles fine and 42703s on the
          # server), that the LEFT JOIN's in_scope flag arrives per generation
          # so an out-of-scope ref can be REPORTED instead of silently
          # vanishing, or that DISTINCT collapses a resource filed into two
          # folders of one scope — without it that generation answers twice.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_promoted_resource_ids_scope_integration.py -v

      - name: Exercise the chat question stamp against the built schema (phase 2a)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place ConversationsAiStore's phase-2a statements execute.
          # A stubbed session cannot show that the newest-agent-message SELECT
          # returns the row the store expects, or that the level-by-level ||
          # merge actually lands the answered stamp when an intermediate key
          # is missing (jsonb_set would return the target unchanged, rowcount 1).
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_conversations_store_question_integration.py -v

      - name: Exercise the paused-issue inbox sweep exclusion against the built schema (phase 2a)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place expire_stale_stmt(skip_paused_issues=True) executes.
          # Its compile test greps the SQL; only Postgres can settle that
          # NOT (kind='issue' AND id IN (SELECT ... WHERE paused_at IS NOT NULL))
          # leaves paused-issue items alone while still expiring the other
          # old orphans (non-issue targets included) and never collapses to
          # "expire nothing".
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_inbox_expire_skips_paused_integration.py -v

      - name: Exercise the chat-uploads folder adoption (mig 450)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # Migration 450 is a single UPDATE whose whole value is in what it
          # does NOT touch: it must adopt exactly one `temp` folder per scope
          # (the oldest, root-level, unkeyed one), leave the rest as plain user
          # folders, and change zero rows on a second run. None of that is
          # checkable without a server — DISTINCT ON, the NOT EXISTS guard and
          # the partial unique index that backs it all only exist in Postgres.
          # The same step also runs the code-side adoption against the real
          # index, which is what proves the migration and `chat_upload.py`
          # agree instead of each minting a row.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_chat_uploads_folder_migration.py -v

      - name: Exercise the prompt-catalog repository against the built schema (mig 455)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place PromptCatalogRepository's ORM statements execute.
          # Its other test compiles them and greps the SQL text, which cannot
          # settle any of: what has_prompt_expr()'s btrim/JSONB clause tree
          # actually admits (a whitespace-only prompt must be OUT, a
          # slide_prompts map with no gen_prompt must be IN — that pair IS the
          # catalog's membership rule), whether DISTINCT really collapses the
          # resource_items fan-out (without it one prompt renders twice),
          # whether slot='examples' really partitions a template's thumbnails
          # from its cover and reference files, or whether the eleven projected
          # columns — prompt_origin among them — all exist.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_prompt_catalog_repository_integration.py -v

      - name: Exercise the workforce dispatch statements against the built schema (2b-2 T3)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place AgentWorkforceRepository's ORM statements execute.
          # Its unit tests hand every statement to a fake session and grep the
          # compiled SQL, which cannot settle any of: whether claim_task's two
          # CAS arms really partition (a fresh queued row claimed, a foreign
          # workflow refused, this workflow's own replay re-admitted WITHOUT
          # knocking in_progress back to assigned), whether UPDATE ... RETURNING
          # a whole entity yields something .scalars().first() can map, whether
          # `metadata ->> 'dispatched_at' IS NULL` really excludes a stamped row
          # from the dispatch list, or whether `coalesce(metadata,'{}') || patch`
          # preserves agent_payload while replacing the stamp. Get any of those
          # wrong and the chain still passes every unit test while dispatching
          # nothing — the failure this whole task exists to prevent.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/integration/test_agent_workforce_repository_orm.py -v

      - name: Exercise generated_media's TEXT origin_run_id against the built schema (3a)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place register_generated_media's INSERT meets a real
          # driver with an int run_id. Its unit test stubs the session and
          # asserts on the COMPILED bind value, which cannot settle the one
          # question that took production down on 2026-09-14: does asyncpg
          # reject an int for the TEXT column origin_run_id? It does —
          # "invalid input for query argument $9: 349441401106307 (expected
          # str, got int)" — and the whole INSERT dies with it, so a generated
          # image registered nothing and had no lineage. The negative control
          # in this file is what keeps the positive case a claim about
          # Postgres rather than about SQLAlchemy's compiler.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_generated_media_int_run_id_integration.py -v

      - name: Assert mig 466's nullable run ids, both CHECKs & the per-call price
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 466 放宽两个 NOT NULL，用两个 CHECK 换回约束。文本测试读迁移写了什么；
          # 只有服务器能回答 CHECK 是否真的拒绝「既无 run 也无人」的行，以及
          # NUMERIC(12,4) 是否真的留住第四位小数。同一步顺带钉住 generated_media
          # 上那个反查索引仍在 —— 3b 正因为它才没有再建一个。（钉的是 456 的
          # uq_genmedia_promoted_resource 一个，不是两个：456 把 307 的
          # idx_genmedia_promoted DROP 掉了，替换而非并存。）
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_migration_466_revert_cost_integration.py -v

      - name: Exercise the revert service against the built schema (3b Task 3)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 上一步问「466 的 CHECK 拦不拦得住坏行」；这一步问「回退真的写出来的那些
          # 行过不过得了」——同一批约束的另一面，而且是唯一一个会改**用户内容**的
          # 产出写入方。单测把 session / 账本 / 登记口全桩掉了，所以只有这里能回答：
          # 带 `FOR UPDATE` 的列表 SELECT、`run_id NULL + actor` 的账本行过不过
          # `script_shot_ops_run_or_actor`、`action='update'` 在不在
          # `script_shot_ops_action_check` 的白名单里、以及登记失败时内容有没有
          # **真的**跟着回滚（桩 session 回滚不了任何东西，而「内容回了、版本没记」
          # 正是血缘要回答的那个问题）。顺带钉住并发回退：SQLSTATE 23505 必须以
          # `IntegrityError.orig.sqlstate` 的形状到达，否则那个 409 退化成 500。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_revert_output_integration.py -v

      - name: Exercise the turn-done watermark reads against the built schema (3b Task 4b)
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # The ONLY place last_transcript_seq / output_keys_for_run execute.
          # Both are aggregates, which is the shape a stubbed session cannot
          # vouch for: whether MAX(seq) over an empty run comes back as None
          # rather than raising or reading 0 ("no events" and "seq 0" are
          # different answers — 0 makes the browser drop the NEWEST frame as
          # the oldest), whether GROUP BY kind, ref_id really collapses two
          # versions of one object into one cache key, and whether
          # ORDER BY MIN(seq) puts a not-yet-stamped registration (seq is
          # NULLABLE — the registry back-fills it) last instead of dropping it.
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_run_seq_and_output_keys_integration.py -v

      - name: Assert mig 472's projection tables, trgm indexes & counter defaults
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 472 建的两张表是 3c 检索与引用反查的全部地基。文本测试读迁移写了什么；
          # 只有服务器能回答：pg_trgm 的 gin_trgm_ops opclass 是否真的可用（没装则
          # CREATE INDEX 直接报错，而文本测试照样绿）、UNIQUE (entity_kind,
          # entity_id) 是否真把同一个对象收成一行、ai_usage_hourly 那五个**加法
          # 累加器**是否真是 NOT NULL DEFAULT 0（可空的话 col + EXCLUDED.col 会把
          # 一整个小时桶算成 NULL），以及两张新表有没有给 anon/authenticated 开策略。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_mig_472_integration.py -v

      - name: Assert mig 475 keyword search honours the filter chips
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 列表路径早就按 chip 过滤，搜索路径一个 chip 都不传——开搜索即整排 chip
          # 静默失效（生产实测：勾「已转录」搜 5000，4 条里只有 1 条转录，UI 显示 4）。
          # 只有服务器能回答：旧签名是否真被 DROP（超集重载并存会让 8 参调用
          # function is not unique）、重建后的 ACL 是否仍不对 anon/authenticated
          # 开放（DROP 会连 463 的 REVOKE 一起丢掉）、以及 SearchService 真实的
          # SQL 串（命名参数 + CAST）是否被 Postgres 接受。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_migration_475_search_chip_filters_integration.py -v

      - name: Assert mig 478 archived notes leave every surface
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 归档要同时从列表、归档视图、标签计数、活跃度日历四个面生效。忘了列表
          # 那条谓词是吵的（笔记明摆着还在）；忘了标签计数那条是静默的——侧栏比
          # 它所标注的列表多数一条，而任何 mock 掉函数的测试都不会发现。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_migration_478_inspiration_archive_integration.py -v

      - name: Assert the search_docs repository's statements on a real server
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 上一步读的是 472 建出来的形状；这一步读 SearchDocsRepository 自己的
          # 语句。它的单测全是 stmt.compile() 字符串断言 —— 编译得过不等于服务器
          # 接受：func.similarity() 要 pg_trgm 真的装着（没装时那些断言照样绿）、
          # on_conflict_do_update 要 search_docs_entity_key 真存在且列对得上（投影
          # 「绝不长出第二行」全挂在它身上）、owner_user_id 与 team_members.user_id
          # 两个 Uuid 列要接受同一个绑定值（绑错是 operator does not exist:
          # uuid = character varying）、ILIKE 的 ESCAPE '\' 要真的让一个字面 %
          # 匹配它自己而不是匹配全表（CLAUDE.md「ILIKE 模式的转义责任要跟着模式走」）。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_search_docs_repository_integration.py -v

      - name: Assert unified_search's two visibility gates on a real server
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 上一步证明 repository 的语句服务器接受；这一步证明**两道门合起来
          # 拦得住人**。/api/v1/search 是跨团队端点，它唯一的安全断言是「别
          # team 的成员搜同一个词，三组全空」——而服务层单测里 visible_issue_ids
          # 是桩的，那套断言只证明「我们在调这道门」，不证明「这道门拦得住」。
          #
          # 两道门各有一条专属用例，职责分界处刻意分开：
          #   * SQL 那道（team_id ∈ 我的 team OR owner_user_id = 我）——别 team
          #     的 bob 搜同一个词拿三组全空。
          #   * Python 那道（visible_issue_ids）——一行 team_id 指着 bob 的 team
          #     （SQL 放行）而 issue_id 指着别 team 的议题（必须拦下）。投影的
          #     坐标列没有外键，这个形状是真的会出现的。
          # 实测两次突变各杀各的那一条（见 task-14 报告）。少了后者，把
          # visible_issue_ids 整个删掉前一条照样绿——那正是这类用例最常见的假绿。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_unified_search_visibility_integration.py -v

      - name: Assert the output_citations repository's statements on a real server
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 引用反查镜像的三条语句（3c Task 16）。同上，单测全是 stmt.compile()
          # 字符串断言：ON CONFLICT DO NOTHING 的「重投不翻倍」全挂在
          # output_citations_message_ref_key 上（索引丢了，第二次会安静地插进重复
          # 行、计数翻倍）；ORDER BY 的决胜键要真的定住同一事务里时间戳完全相等的
          # 两条；GROUP BY 分错组的表现是血缘面板上每一版都显示整条链的引用数。
          # message_id 的外键还要求夹具建一条真消息 —— 这一条也只有真库能拒。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_output_citations_repository_integration.py -v

      - name: Assert the tree-charge settle CAS on a real server
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 积分收口的幂等（积分 Task 3）。单测里的 session 是桩的，这三件事
          # 只有真服务器能判：① CAS 的 where 要同时匹配「cost 层在」与「不在」
          # 两种形状 —— 不匹配就是第一次收口抢不到、整棵树一分不扣，日志只说
          # already；② values 的两层 jsonb || 写错不报错，只会把 cost 视图连同
          # 四道花费一起冲掉，而钱已经扣了（本步骤上线时就抓到过一次：
          # cast("{}", JSONB) 绑成 jsonb 字符串标量，|| 退化成数组拼接）；
          # ③ 取树的 id = root OR root_run_id = root 要一次捞全三代。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_tree_charge_cas_integration.py -v

      - name: Assert refunds offset the charged-points readers on a real server
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 终审 I6：退款行必须从「这次回合扣了多少」里减掉，否则已退款的树在
          # 议题线程 / 聊天气泡 / done 状态帧三处仍然显示 ◇ n —— 界面说扣了、
          # 账上已经退了（2026-09-17 的 81 棵树就是这个形状）。三件事只有真
          # 服务器判得了：① 两条腿各自的 type/reference_type 谓词真的选得出行
          # （桩 session 按调用顺序发牌，谓词写错它照样绿）；② consume 负 /
          # refund 正这两个符号是 mig 123 那个 RPC 的行为，不是代码里的约定，
          # 减法方向只能由真写进表里的行来证；③ amount 是 INTEGER 而不是
          # numeric。顺带把 charged_points_for_run_trees 一起过一遍 —— 那是三个
          # 宿主真正读的那一层。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_points_refund_offset_integration.py -v

      - name: Assert the search_docs body backfill against a real server
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 存量正文回填（3c 补充票）。mig 472 的产出回填段没有写 body 那一列，
          # 所以 3c 之前登记的产出在检索里只有标题搜得到。正文按 kind 各自重建：
          # 分镜/场次走 deliverables/diff.py 的真账本折叠（jsonb + 三档字段解析，
          # 全在服务器那边），媒体取 generated_media.prompt。单测把 rebuild_content
          # 换成了替身，所以整条链在真库上一次都没跑过。
          # 只有真库能答的四件：两个候选集查询的三列/BIGINT 连接编译得过≠跑得通；
          # 「永不覆盖」整个挂在 UPDATE 的 body IS NULL OR body='' 谓词上（桩 session
          # 对任何谓词都点头）；回填不碰 updated_at —— 动了它，几个月前的产出就会
          # 排到 idx_search_docs_team_updated 的最前面；以及重跑一次必须是 no-op。
          # 三次突变实测各杀各的：摘谓词只杀竞态那条（候选集用例照样绿，所以两条
          # 都要有）、顺手 bump updated_at 杀时间戳那条、自己拼一个渲染器杀
          # 「与实时写方同一串」那条。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_search_docs_body_backfill_integration.py -v

      - name: Assert the own_cost_cents backfill against a real server
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # mig 479 的三档回填 + 子 run 的 issue_id 补齐（3d 第 0 票 PR-A）。这条
          # 迁移没有新对象可供 schema diff 比对行为 —— 它全部的内容就是四条
          # UPDATE 的谓词，而谓词恰好是桩 session 唯一看不见的东西（它按调用顺序
          # 发牌，WHERE 写成什么都点头）。四件只有真库能判：
          # ① tier 1 的 jsonb 取值 —— own_cents + media_cents castable，且
          #    by_child（后代的钱，正是新列要甩掉的那部分）没有被顺手加进来；
          # ② tier 2 的 NOT EXISTS —— 它是「有后代的行别碰老列」的全部实现；
          # ③ tier 3 是一次「不写」，只有在另外两档真的跑过的库上才观察得到；
          # ④ own_cost_cents IS NULL 守卫 —— PR-B 让写方实时写这一列之后，一次
          #    迁移重跑会把正确的自身花费覆盖成含后代的 cost_cents；
          # ⑤ 第④段的 issue_id 与 conversation_id 走**各自**的 OR 臂和各自的
          #    COALESCE，所以两者必须各有一个用例：只造「两列同时为 NULL」的行
          #    分不出这两条臂，摘掉 conversation_id 赋值照样绿。
          # 六次突变实测各杀各的：摘 NOT EXISTS 杀 tier 3、tier 1 加总 by_child 杀
          # tier 1、摘 IS NULL 守卫杀「重跑不覆盖」、摘 issue_id 赋值杀 issue_id
          # 继承、摘 conversation_id 赋值与把第二条 OR 臂写成 r.issue_id 各杀
          # conversation_id 继承（那条用例刻意造非对称行：子 run 自己已有
          # issue_id、只有 root 有会话绑定，所以唯一能选中它的就是第二条臂）。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_migration_479_own_cost_backfill.py -v

      - name: Assert the own_cost_cents tree invariants against a real server
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 3d 第 0 票 PR-B：六个读面从 cost_cents（自身 + 已报到的后代）换成
          # own_cost_cents（只记自身）。一棵真的 4 层树（root / 进程内子 / 后台子 /
          # 两层 Delegate 子，跨两个 agent），spec §3 不变量 1–4 全部对着手算常数
          # 断言（5 / 6 是源码扫描，不在这里）。桩 session 看不见的六件：
          # ① 三条语句的 WHERE 与 GROUP BY 毫无共同之处（树按
          #    COALESCE(root_run_id, id)、议题按 issue_id OR conversation_id、agent
          #    按 agent_id 开时间窗），却必须给同一个数 —— 桩按调用顺序发牌，三条
          #    都写错成同一个错数照样绿；
          # ② COALESCE(root_run_id, id) 这个树键是服务器在算：写成 root_run_id 丢
          #    掉 root 自己那份，写成 id 把一棵树拆成五棵，两种在桩里都「成功」；
          # ③ 委派子 run 进不进预算就是一条 WHERE 的事（旧语句的
          #    parent_run_id IS NULL 正是本票要拆的），只能靠真删两行看差值；
          # ④ 门禁那道 prior + live 不许双计 —— prior 必须恰好是 B..E 的自身之和；
          # ⑤ 效率页那条子查询按「属于某棵在 scope 内的树」收行、不按每行自己的
          #    scope 列过滤（裁定 7）。夹具为此造 scope 不对称（D、E 的 team_id 是
          #    NULL，真实形状：子 run 的 scope 是 best-effort）；
          # ⑥ §3-4 崩溃行不归零 —— 真的跑一遍 mark_heartbeat_lost_ids，看那条终态
          #    UPDATE 有没有碰 own_cost_cents。此前这句承诺只写在
          #    tests/runner/test_own_cost_column_writers.py 的散文里，没有测试。
          # 四次突变实测各杀各的：_own_cost_sum_stmt 指回 cost_cents 杀三条（不变量
          # 1 / 3 / 4）、_USAGE_COST_COL 指回 cost_cents 杀按 agent 那一步、
          # tree_cost_cents 指回 cost_cents 杀不变量 1 与 2、给效率页子查询补上行级
          # scope 过滤杀效率页那条（35.75 变 21.75）。⚠️ 最后一次突变在 scope 列一
          # 致的夹具上**不会**转红 —— 那个不对称是必需的，别把它「整理」掉。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_own_cost_tree_invariants_integration.py -v

      - name: Replay mig 488's mediahub → nous data renames on production-shaped rows
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 迁移已在上面随「above the watermark」那步真跑过（空库上），这里在回滚事务里
          # 用生产形状的夹具把文件本体再放两遍：7 条 mediahub-* 目录行 + 一对孪生、
          # task_assignment 的 nous: 形态改而裸值不改（裸值改了会让 ai_router 的
          # startswith("nous-") 开始扣费）、disabled_models 与 fallback_models 保序、
          # 图集 MIME 改了而 updated_at 不动、第二遍什么都不变、只删 VIEW 不删同名表。
          # 四次突变实测各杀各的：去掉 colon 限定杀 user_settings 那条、去掉 SET LOCAL
          # replica 杀 updated_at 那条、去掉孪生守卫杀改名那组、放宽 relkind 杀两条删视图用例。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_migration_488_rename_data_integration.py -v

      - name: Assert mig 491 closed the browser path to backend-only SECURITY DEFINER RPCs
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 8 个后端专用 definer 函数（两个积分、find_duplicate_videos、三个 cleanup、
          # get_user_tag_counts、rpc_reorder_storyboard_frames）：anon / authenticated
          # 必须没有 EXECUTE，postgres 必须保留（负向对照：全收回会让扣分静默断掉）。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_migration_491_revoke_definer_rpcs_integration.py -v

      - name: Assert mig 496 made the RLS identity helpers answer only for the caller
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # is_conversation_member / conversation_member_joined_at / get_user_team_ids(_text)：
          # anon（带 / 不带 JWT）与 authenticated 替别人问一律拿空；自己问、postgres 与
          # service_role 拿真答案（负向对照）；引用它们的策略仍只放行调用者自己的行。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_migration_496_rls_helper_identity_guard_integration.py -v

      - name: Assert mig 497's resource_embeddings (halfvec + HNSW) and its repositories
        working-directory: backend
        env:
          INTEGRATION_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/drift
        run: |
          set -euo pipefail
          # 列真是 halfvec(2048)、HNSW 用 halfvec_cosine_ops、RPC 只在一个空间里排序、
          # anon / authenticated 没有 EXECUTE；再经真 ORM 引擎跑两个 repository
          # （upsert 原地替换、insert-or-ignore 的空间键、coverage 与缺行反连接）。
          bash "$GITHUB_WORKSPACE/.github/scripts/pytest-no-full-skip.sh" tests/db/test_migration_497_resource_embeddings_integration.py -v
